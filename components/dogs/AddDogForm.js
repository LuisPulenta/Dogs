import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

export default function AddDogForm({toastRef,setLoading}) {
    return (
        <View>
            <Text>AddDogForm...</Text>
        </View>
    )
}

const styles = StyleSheet.create({})